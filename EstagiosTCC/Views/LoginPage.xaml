<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:EstagiosTCC.ViewModels"
             mc:Ignorable="d"
             x:Class="EstagiosTCC.Views.LoginPage"
             Title="{Binding Title}"
             NavigationPage.HasNavigationBar="false">
    <ContentPage.BindingContext>
        <vm:LoginViewModel/>
    </ContentPage.BindingContext>
    <StackLayout Orientation="Vertical" Padding="30" Spacing="40">
        <StackLayout Orientation="Vertical" Spacing="10">
            <Entry x:Name="txtEmail" Text="{Binding Email}" Placeholder="Email"   
                       HeightRequest="40"   
                       Keyboard="Email" />
            <Entry x:Name="txtPassword" Text="{Binding Password}" Placeholder="Senha"   
                       HeightRequest="40"   
                       IsPassword="True" />
            <Button x:Name="btnLogin" Command="{Binding LoginCommand}" Text="Login" />
            <Button x:Name="btnCreateAccount" Command="{Binding CreateAccountCommand}" Text="Criar Conta" />
            <Button x:Name="btnRecoveryPassword" Command="{Binding RecoveryPasswordCommand}" Text="Recuperar Senha" />
        </StackLayout>
    </StackLayout>
</ContentPage>